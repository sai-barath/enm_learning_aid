#pragma once
#include <cmath>
#include "vectorR3.hpp"

class customRod {
    private:
        double thickness;
        double length;
    vectorR3 computeAtPoint(double x, double y);
};

vectorR3 customRod::computeAtPoint(double x, double y) {
    vectorR3 pointVec;

    return pointVec;
}